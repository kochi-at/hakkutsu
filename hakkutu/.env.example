# Never prefix these secrets with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Optional shared passphrase for a small, private hackathon demo.
# Set this before enabling the paid AI API on a public URL.
APP_ACCESS_TOKEN=
